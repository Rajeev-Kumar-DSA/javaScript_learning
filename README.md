# javaScript_learning

If we will use the online github compiler - then github will be provide me one compiler (without any software)
here we need to download the software inside the compiler (only one time)
download the software inside the Github compiler
1. three line left above corner
2. go to View tab
3. then select - Command Platter (cntrl + command + P)
4. then will get the search bar box -> container -> after that we have to select the popup (Add Dev container Configuration files)
5. (using 4th step - virtual screen can able to add the user requirment software like - node, python, java)
6. then we need to search the - Node (here again have to select the "show all Defination", because directly will not come)
7. then we can able to add the required software
8. then will get the version option also - we can select any one of them(suggestion - choose 1st or 2nd option - latest one)
